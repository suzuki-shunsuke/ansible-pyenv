---
- hosts: all
  roles:
  - git
  - role: ansible-pyenv
    # pyenv_rc_path: "{{ ansible_env.HOME }}/.bashrc"
    pyenv_rc_path: "{{ ansible_env.HOME }}/.profile"
    pyenv_root: ~/pyenv
    pyenv_is_dependencies_installed: yes
